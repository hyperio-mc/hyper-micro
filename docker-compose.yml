version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - STORAGE_PATH=/app/data/storage
      - LMDB_PATH=/app/data/lmdb