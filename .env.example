# hyper-micro environment configuration

# Server configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Data storage paths
LMDB_PATH=./data/lmdb
STORAGE_PATH=./data/storage

# API Keys (comma-separated)
API_KEYS=dev-key-change-in-production

# Admin Authentication (JWT-based)
# ADMIN_EMAIL - admin user email for login
# ADMIN_PASSWORD - bcrypt hash of the password (generate with: npx bcryptjs-cli hash "your-password")
# JWT_SECRET - secret key for signing JWT tokens (use a strong random string)

ADMIN_EMAIL=admin@example.com
# Example: bcrypt hash of "secret123" - generate your own!
ADMIN_PASSWORD=$2a$10$YourBcryptHashHere
JWT_SECRET=your-jwt-secret-change-me

# To generate a bcrypt password hash:
# npx bcryptjs-cli hash "your-password"
# or use Node.js:
# node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('your-password', 10));"